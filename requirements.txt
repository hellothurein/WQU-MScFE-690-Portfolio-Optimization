# Environment of record: Google Colab, Python 3.12 (CPU), May 2026 run
# Versions below match the environment that produced the reported results
# (verified from the executed notebook's pip output).
stable-baselines3[extra]==2.9.0
gymnasium==1.3.0
numpy==2.0.2
torch==2.11.0
pandas==2.2.2
matplotlib==3.10.0
# Supporting libraries (used by the pipeline; exact run versions not
# captured in the notebook output — any recent release is compatible):
yfinance>=0.2.50
scipy>=1.13
seaborn>=0.13
